name: docs_gee
description: Pure Dart library for generating DOCX and PDF documents with rich formatting, tables, lists. Cross-platform with no native dependencies.
version: 1.2.0

homepage: https://github.com/erykkruk/docs_gee
repository: https://github.com/erykkruk/docs_gee

topics:
  - docx
  - pdf
  - document
  - word
  - generator

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  archive: ^4.0.0

dev_dependencies:
  flutter_lints: ^4.0.0
  test: ^1.24.0
